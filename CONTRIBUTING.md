# 貢獻指南

感謝協助改善 Rocket Workspace。提交變更前請先確認：

- 不要加入 `workspace-data/`、`backups/`、`.env`、帳號資料或附件。
- 執行型別檢查、測試與正式建置。
- 資料庫變更必須新增 Prisma migration，不能重新改回啟動時的 schema 同步。
- 文件、權限、備份與協作相關改動應附上可重現的驗證步驟。
- 不要在程式、文件或 Issue 中貼出密碼、權杖、私鑰或實際飛行資料。

本專案採用 AGPL-3.0-or-later。提交程式碼即表示你有權依此授權提供該貢獻。
